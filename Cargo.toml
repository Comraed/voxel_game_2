[package]
name = "voxel_game_2"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.16.1"}#, features = ["dynamic_linking"] }
noise = "0.9.0"

[lints.clippy]
too_many_arguments = "allow"